/* jenner-check autoexec for t001_uva_freq_means */
/* cap input rows for the captured run */
options obs=100;

/* This bundle's script.sas is self-contained (it builds its own WORK dataset via
   inline datalines), so it also runs standalone against /v1/quick. */
